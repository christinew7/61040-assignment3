{
  "name": "filetracker-concept",
  "version": "1.0.0",
  "description": "A AI-augmented concept to track file progress",
  "main": "dist/filetracker-tests.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/filetracker-tests.js",
    "dev": "ts-node --transpile-only filetracker-tests.ts",
    "test": "npm run build && node dist/filetracker-tests.js"
  },
  "keywords": ["gemini", "ai", "schedule", "student", "typescript"],
  "author": "Christine Wu",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
